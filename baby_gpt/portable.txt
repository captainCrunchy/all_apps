import numpy as np

# Hyperparameters
vocab_size = len(chars)
embedding_dim = 32   # We're keeping it small on purpose

# Create embedding matrix (random initialization)
np.random.seed(42)
embeddings = np.random.randn(vocab_size, embedding_dim).astype(np.float32)

print("Embedding matrix shape:", embeddings.shape)

# Let's see what one character looks like
char_index = stoi print("Embedding vector for 't':", embeddings [:8])
